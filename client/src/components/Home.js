import React from "react";
import "../css/home.css";
import Layout from "./Layout";
const Home = ({children}) => {
  return (
    <div class = "home-body">
      <Layout>

      </Layout>
    </div>
  );
};

export default Home;
